# requirements.txt — CeSRR Inverse Design Framework
# Pin major versions; minor updates allowed.

# Core scientific stack
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
openpyxl>=3.1          # XLSX parsing

# Machine learning
scikit-learn>=1.3
torch>=2.1.0           # PI-cVAE, Tandem, MDN
gpytorch>=1.11         # optional: GPU-accelerated GP (fallback to sklearn)

# Bayesian optimisation / acquisition functions
botorch>=0.9           # optional: advanced EI / BO (GP-based AL)

# Interpretability
shap>=0.44

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Experiment tracking / utilities
tqdm>=4.65
joblib>=1.3            # model serialisation

# Design of experiments
pyDOE2>=1.3            # Latin Hypercube Sampling (alternative to scipy.stats.qmc)

# Notebook / Colab
ipywidgets>=8.0        # progress bars in Jupyter
