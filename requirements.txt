# requirements.txt — CeSRR Inverse Design Framework
#
# COLAB USAGE NOTE:
#   Colab ships with numpy 2.x, pandas 2.x, scipy, torch and matplotlib
#   pre-installed. Do NOT pin numpy < 2.0 — it will downgrade and break
#   pre-compiled C extensions (pandas, scipy) with a binary incompatibility
#   error ("numpy.dtype size changed").
#
#   The colab_runner.py section3 only installs packages that are NOT bundled
#   with Colab (shap, pyDOE2, botorch, gpytorch) using --no-deps where safe.
#   Everything else (numpy, pandas, torch, scipy, sklearn, matplotlib,
#   seaborn, openpyxl, joblib) is already present in Colab.

# ─── Packages NOT pre-installed in Colab (always install) ───────────────────
shap>=0.44
pyDOE2>=1.3            # Latin Hypercube Sampling
gpytorch>=1.11         # GPU-accelerated GP kernels
botorch>=0.9           # Bayesian optimisation / EI acquisition

# ─── Packages that ARE in Colab — only listed for local/non-Colab runs ──────
numpy>=1.24            # NO upper bound — Colab has numpy 2.x; don't downgrade
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
torch>=2.1.0
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1
tqdm>=4.65
joblib>=1.3
ipywidgets>=8.0
